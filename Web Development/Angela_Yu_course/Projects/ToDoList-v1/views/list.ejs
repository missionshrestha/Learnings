<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ToDo List</title>
  </head>
  <body>
    <% if (kindOfDay==="Saturday"||kindOfDay==="Sunday") { %>
    <h1 style="color: blue"><%= kindOfDay %> ToDo Lists</h1>
    <% } else { %>
    <h1 style="color: red"><%= kindOfDay %> ToDo Lists</h1>
    <% }%>

    <form action="/" method="post">
      <input type="text" name="newItem" id="" placeholder="Enter what to Do!" />
      <button type="submit">Add List</button>
    </form>
    <ul>
      <% for( let index = 0; index < newItemListArray.length; index++ ) { %>

      <li><%= newItemListArray[index] %></li>
      <% } %>
    </ul>
  </body>
</html>
